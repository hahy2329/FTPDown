<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">

	<select id="checkDuplicatedId" parameterType="String" resultType="String">
		SELECT
			NAME
		FROM
			USER
		WHERE
			NAME = #{name}
	</select>
	
	<select id="checkDuplicatedEmail" parameterType="String" resultType="String">
	 	SELECT
	 		EMAIL
	 	FROM
	 		USER
	 	WHERE
			EMAIL = #{email}
	</select>
	
	<insert id="insertRegister" parameterType="UserDTO">
		INSERT INTO USER(NAME, NICKNAME, PASSWORD, EMAIL, ZIPCODE, ROAD_ADDRESS, JIBUN_ADDRESS, NAMUJI_ADDRESS, ROLE)
		VALUES(
			#{name},
			#{nickname},
			#{password},
			#{email},
			#{zipcode},
			#{roadAddress},
			#{jibunAddress},
			#{namujiAddress},
			#{role}
		)
	
	</insert>
	
	<select id="loginCheckId" parameterType="String" resultType="UserDTO">
		SELECT
			*
		FROM
			USER
		WHERE
			NAME = #{name}
	</select>
</mapper>