<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
             http://www.springframework.org/schema/beans/spring-beans.xsd
             http://www.springframework.org/schema/security
             http://www.springframework.org/schema/security/spring-security.xsd">
             
             
             <http pattern="/**/login" security="none"></http>
             <http pattern="/**/register" security="none"></http>
             
             <http auto-config="true" use-expressions="true">
             <csrf disabled="true"/>
             
             <intercept-url pattern="/**/jquery-3.4.1.min.js" access="permitAll"/>
             <intercept-url pattern="/**" access="hasRole('ROLE_USER')"/>
             
             <!-- login-page : 로그인 페이지, username-parameter : 로그인 시 아이디 데이터를 담을 변수, password_parameter : 로그인 시 패스워드 데이터를 담을 변수
             login-processing-url : 로그인 처리 url, default-target-url : 로그인 성공 후 요청 할 url, authentication-failure-url : 로그인 실패 시 요청할 url
             always-use-default-target : 로그인이 성공할 경우 무조건 지정된 default-target-url 페이지로 이동하도록 설정하는 것입니다. -->
             
             <form-login
             login-page="/user/login"               
             username-parameter="name"
             password-parameter="password"
             login-processing-url="/login.do"
             default-target-url="/user/loginSuccess"
             authentication-failure-url="/user/login"
             always-use-default-target="true"
             
             />
             
             </http>
             
             <authentication-manager>
             	<authentication-provider ref="userAuthenticationProvider"/>
             </authentication-manager>
             <beans:bean id="userAuthenticationProvider" class="com.application.FTP.User.service.UserAuthenticationProvider"/>
</beans:beans>             